[project]
name = "security"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "anurag.ambekar1", email = "anurag.ambekar@textbranch.com" }
]
requires-python = ">=3.14"
dependencies = [
    "email-validator>=2.3.0",
    "fastapi>=0.141.1",
    "pwdlib[argon2]>=0.3.1",
    "pyjwt>=2.13.0",
    "storage",
]

[build-system]
requires = ["uv_build>=0.10.9,<0.11.0"]
build-backend = "uv_build"

[tool.uv.sources]
storage = { workspace = true }
