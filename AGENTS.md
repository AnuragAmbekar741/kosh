# Agent guidelines

- Read [docs/README.md](docs/README.md) first.
- Follow [`.cursor/rules/`](.cursor/rules/) — uv, alembic, ponytail, docs-for-agents.
- Python deps: `uv add`. Migrations: `alembic revision --autogenerate`.
- Frontend later: `apps/web` (pnpm), not a uv member.
